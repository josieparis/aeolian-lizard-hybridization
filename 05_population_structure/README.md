# 05_population_structure/

Shell steps run on the server (`01`–`04`), R plotting run locally. `plot_admixture_results.R` was later re-used for
another project and its `setwd()` reflects that; the plotting logic is the one used for Figure 1.
`plot_fineRADstructure.R` sources `FinestructureLibrary.R` from a local fineRADstructure checkout.
